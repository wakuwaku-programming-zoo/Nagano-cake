<div class="container mt-4" style="padding-bottom: 70px;">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <h2>商品詳細</h2>

      <div class="item-details">
          <%= image_tag @item.get_image, class: "item-image" %>

        <p><strong>商品名:</strong> <%= @item.name %><p>
        <p><strong>商品説明:</strong><%= @item.introduction %></p>
        <p><strong>ジャンル:</strong> <%= @item.genre.name %></p>
        <p><strong>税込価格（税抜価格）:</strong> <%= number_to_currency(@item.price * 1.1, unit: "円") %>（<%= number_to_currency(@item.price, unit: "円") %>）</p>
        <p><strong>販売ステータス:</strong> <%= @item.is_sales_status ? "販売中" : "販売停止中" %></p>

        <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success" %>
      </div>
    </div>
  </div>
</div>
